# Using This

```
const {AndesiteClient} = require("andesite.js");

const andeclient = new AndesiteClient({
    host: 192.168.1.1:2333 // Host Adrress of andesie node
    password: 'you can omit this if you havent set an password'
});
```